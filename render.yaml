services:
  # Type: web
  name: gmrit-results
  env: python
  plan: free
  buildCommand: pip install -r requirements.txt
  startCommand: python run.py
  healthCheckPath: /
  envVars:
    - key: PYTHON_VERSION
      value: 3.9
